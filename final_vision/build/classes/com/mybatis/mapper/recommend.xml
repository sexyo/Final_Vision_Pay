<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="recommendMapper">

	<resultMap type="java.util.Map" id="rec_card">
		<result property="rec_card" column="rec_card"/>
	</resultMap>
	<select id="myRecommendCard" statementType="CALLABLE"  parameterType="map">
		 {call proc_myRecommandCard(#{mem_id, mode=IN, jdbcType=VARCHAR, javaType=java.lang.String}
  					  	 		  , #{rec_card, mode=OUT, jdbcType=CURSOR, javaType=java.sql.ResultSet, resultMap=rec_card}
  					  	  )} 
	</select>
	

	
</mapper>
